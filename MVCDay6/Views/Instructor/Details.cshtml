@model InstructorViewModel
@{
    ViewData["Title"] = "Details";
    ViewBag.Hide = false;
}
<section>
    <div class="container py-4">
        <div class="row d-flex justify-content-center">
            <div>
                <h1 class="text-info text-center"> Instructor Details </h1>
                <h2 class="text-warning text-center my-3">@Model.Name</h2>
            </div>
        </div>
        <div class="row d-flex justify-content-center">
            <div class="col-md-3">
                <partial name="_InstructorPartialView" />
            </div>
        </div>
    </div>
</section>
 